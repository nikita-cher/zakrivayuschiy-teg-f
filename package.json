{
  "name": "zakrivayuschiy-teg-f",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "push": "git add -A & git commit & git push",
    "fixAndPush": "npm run fixAll & npm run push",
    "check": "prettier --check './**/*.{html,css}'",
    "format": "prettier --write './**/*.{html,css}'",
    "lint": "stylelint \"**/*.css\"",
    "lintfix": "stylelint \"**/*.css\" --fix",
    "checkAll": "npm run check & npm run lint",
    "fixAll": "npm run format & npm run lintfix"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "prettier": "^3.2.5",
    "stylelint": "^16.6.0"
  }
}
